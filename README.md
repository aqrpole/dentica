## OpenEMR-Dentica
A forked project from OpenEMR to build Dental Patient Management System

# Build the project (changes visible)
docker compose up -d

# Build Interactively to see live logs
docker compose up --build
